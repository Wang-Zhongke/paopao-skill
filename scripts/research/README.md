# 研究工具

此目录用于来源整理、字幕采集、OCR、清洗与质量检查，不是安装 Skill 或运行聊天室的前置步骤。

从 Skill 根目录运行所需脚本，例如 `python3 scripts/research/search_corpus.py --help`。原始字幕不随公开包分发；部分工具依赖本地原始资料、网络服务或 macOS OCR 能力，请先查看对应脚本的参数与依赖。

普通使用只需上一级的 `install.py`；内容检查使用 `validate_skill_content.py`。
